import { BaseCard } from "vlocity_cmt/baseCard";                    import { LightningElement, api, track } from "lwc";                    import data from "./definition";                    export default class cfINWIB2C_LmInformationsBillingAccount extends BaseCard(LightningElement) {                        connectedCallback() {                            this.setDefinition(data);                        }                    }