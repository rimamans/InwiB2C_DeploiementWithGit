import { masterLayout } from "vlocity_cmt/masterLayout";                                import { LightningElement, api, track } from "lwc";                                import data from "./definition";export default class cfInwiB2C_Create_Case_From_Billing_Account_Community extends masterLayout(LightningElement) {@api recordId;@api theme = 'slds';@api debug;connectedCallback() {                            super.connectedCallback();                            this.definition = data;}}