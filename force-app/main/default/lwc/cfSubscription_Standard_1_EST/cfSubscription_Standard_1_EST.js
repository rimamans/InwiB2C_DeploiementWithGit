import { BaseCard } from "vlocity_cmt/baseCard";                    import { LightningElement, api, track } from "lwc";                    import data from "./definition";                    export default class cfSubscription_Standard_1_EST extends BaseCard(LightningElement) {                        connectedCallback() {                            this.setDefinition(data);                        }                    }